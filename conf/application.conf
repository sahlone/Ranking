play {
  filters {
    enabled = []
    hosts {
      allowed = ["."]
    }
  }

  modules {
    enabled += modules.GuiceConfigModule
  }
}

ranking-service {
  env = ${?ENV}
  env = "Dev"

  remoteRankingSerice {
    serviceEndpoint = ${?RANKING_ENDPOINT}
  }

}

